def chat_with_feedback(report, chat_history, user_input):
    # Placeholder: 실제 GPT 모델과 연동하여 응답을 생성하는 로직을 구현하세요.
    # 예시로 간단한 문자열을 반환합니다.
    response = f"ChatGPT: {report} (사용자 입력: {user_input}))"
    print(f"chat_history: {chat_history}")
    return response